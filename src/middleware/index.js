/**
 * Point of contact for middleware modules.
 * @module middleware
 * @example import { api, stream } from 'middleware';
 */

export api from './api';
export piwik from './piwik';
export stream from './stream';
